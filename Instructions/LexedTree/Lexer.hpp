# pragma once


namespace alfie{
	struct token_type{

	};

	struct lexem{
		struct token_type token;
	};
	

	class Lexer{
		// --- Head
	public:
	private:

		// --- Body
	public:
		lexed_tree lex(){ }

		// --- CTOR ~ DTOR
	public:
		// --- Constructor ---
		// -- Default constructor --
		Lexer(){ }

		// --- Destructor ---
		// -- Default destructor --
		~Lexer(){ }
	};
};

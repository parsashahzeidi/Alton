# pragma once


namespace alfie{
	class {
		// --- Head
	public:
	private:

		// --- CTOR ~ DTOR
	public:
		// --- Constructor ---
		// -- Default constructor --
		(){ }

		// --- Destructor ---
		// -- Default destructor --
		~(){ }
	};

	class {
		// --- Head
	public:
	private:

		// --- Body
	public:

		// --- CTOR ~ DTOR
	public:
		// --- Constructor ---
		// -- Default constructor --
		(){ }

		// --- Destructor ---
		// -- Default destructor --
		~(){ }
	};
};
